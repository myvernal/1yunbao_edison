<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@id/chat_id_time_container"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_horizontal"
    android:layout_marginBottom="15.0dip"
    android:layout_marginTop="15.0dip"
    android:gravity="center"
    android:visibility="visible" >

    <TextView
        android:id="@id/chat_id_time"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_gravity="center"
        android:layout_marginLeft="5.0dip"
        android:layout_marginRight="5.0dip"
        android:textColor="@color/font_value"
        android:textSize="12.0sp" />

    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="1.0dip"
        android:layout_centerVertical="true"
        android:layout_marginLeft="55.0dip"
        android:layout_toLeftOf="@id/chat_id_time"
        android:background="@color/timestampline"
        android:contentDescription="@null" />

    <ImageView
        android:layout_width="fill_parent"
        android:layout_height="1.0dip"
        android:layout_centerVertical="true"
        android:layout_marginRight="55.0dip"
        android:layout_toRightOf="@id/chat_id_time"
        android:background="@color/timestampline"
        android:contentDescription="@null" />

</RelativeLayout>